# Dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# Testing
/coverage

# Screenshot loop working surface — regenerated on demand, never reviewed as a
# diff. The visual-regression baselines under tests/**-snapshots/ ARE committed,
# since a baseline nobody can diff against is not a baseline.
/.shots/
/test-results/
/playwright-report/

# Next.js
/.next/
/out/

# Production
/build

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files — NEVER commit these
.env*.local
.env

# Unredacted product screenshots containing real patient names, message content
# and presenting conditions. Kept locally for redaction work only — NEVER commit.
/private-assets/

# Vercel
.vercel

# TypeScript
*.tsbuildinfo
next-env.d.ts
